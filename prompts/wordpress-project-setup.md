# WordPress project setup task

Inputs: local/staging target, allowed files, WordPress/PHP/theme/Elementor versions, Pro availability, task scope, backup location.

Inspect the provided environment and report missing prerequisites. Do not print credentials or read unrelated client folders. Establish where project code, templates, and private exports belong. Keep runtime configuration and assets outside this public toolkit.

Produce a native-element plan for the requested page, the commands or UI checks needed to record the baseline, and a rollback path. Confirm installed capabilities before proposing Pro or third-party features. Do not install or upgrade plugins merely to make an assumption true.

Deliver a setup record, a small next task, and explicit blockers. Mark checks as executed only when there is output or observed UI evidence.
